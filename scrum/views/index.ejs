<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Login Form Tutorial</title>
    <style>
        .login-form {
            width: 300px;
            margin: 0 auto;
            font-family: Tahoma, Geneva, sans-serif;
        }

            .login-form h1 {
                text-align: center;
                color: #4d4d4d;
                font-size: 24px;
                padding: 20px 0 20px 0;
            }

            .login-form input[type="password"],
            .login-form input[type="text"] {
                width: 100%;
                padding: 15px;
                border: 1px solid #dddddd;
                margin-bottom: 15px;
                box-sizing: border-box;
            }

            .login-form input[type="submit"] {
                width: 100%;
                padding: 15px;
                background-color: #535b63;
                border: 0;
                box-sizing: border-box;
                cursor: pointer;
                font-weight: bold;
                color: #ffffff;
            }
            form{
                font-size: 15px;
            }
            .login-form input[type="radio"]{
                padding-bottom: 15px;
            }
    </style>
	<script>
	function validation() {
     var uname = document.forms["login"]["username"].value;
     var pass= document.forms["login"]["password"].value;
	 var rol= document.forms["login"]["role"].value;
	
	  result.forEach(function(result){
	  if(uname== result.username && pass== result.password){
	      document.write("success");
	  }
	  })
	 }
	</scrpt>
</head
<body>
    <div class="login-form">
        <h1>Login Form</h1>
	
        <form action="" method="POST" name="login" onsubmit="return validation">
            <input type="text" name="username" placeholder="Username">
            <input type="password" name="password" placeholder="Password">
            Role     <input type="radio" name="role" value="owner" />Product owner
            <input type="radio" name="role" value="developer" />Developer
            <input type="submit" value="SUBMIT">
        </form>
    </div>
</body>
</html>